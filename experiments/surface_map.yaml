checkpointing :
  checkpoint_path    : './outputs/neural_map/'
  prefix : 'overfit'

dataset :
  sample_path : '/SET/YOUR/PATH/HERE/'
  num_workers : 4
  num_points  : 4000
  num_epochs  : 50000


hydra:
  run:
    dir: './'
